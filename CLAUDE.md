Run the project with `uv run findcognates2.py`.
This is fairly cheap (60s), and you should feel free to do it to test that your changes work.

Note: The kaikki.org data is big (23GB) so be careful wrt to scanning.